const computer = {

    brand : 'apple',
    model : 'mac m3',
    processor : 'm3 cheap',
    ssd : '512gb',
    serial_no: 1220,

}

//Use Object.keys() → to get the names of the properties
//Use Object.values() → to get the values of the properties

const values = Object.values(computer);
console.log(values);