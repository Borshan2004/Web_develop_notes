import React from 'react';

const EachAppoiment = ({apdata}) => {
    return (
        <div>
            <div>
                <h1>{apdata.name}</h1>
            </div>
        </div>
    );
};

export default EachAppoiment;